# ═══════════════════════════════════════════════════════════════════════════════
# OptimalSystemAgent — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#
#   cp examples/.env.example .env
#
# Only the LLM provider section is required to start.
# Everything else is optional and can be added incrementally.
# ═══════════════════════════════════════════════════════════════════════════════


# ── LLM Provider ────────────────────────────────────────────────────────────
#
# Which LLM backend should OSA use?
# Options: ollama | anthropic | openai | groq
#
# Start with ollama for a fully local, free setup.
# Switch to a hosted provider for higher capability or multi-device access.

OSA_PROVIDER=ollama

# Ollama Cloud (hosted — requires OLLAMA_API_KEY from https://ollama.com)
OLLAMA_URL=https://ollama.com
OLLAMA_MODEL=nemotron-3-super:cloud
OLLAMA_API_KEY=

# Ollama local (free — requires Ollama running at OLLAMA_URL)
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2:latest

# Anthropic (hosted — requires ANTHROPIC_API_KEY)
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=your-model-here

# OpenAI (hosted — requires OPENAI_API_KEY)
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o

# Groq (hosted, fast inference — requires GROQ_API_KEY)
# GROQ_API_KEY=gsk_...
# GROQ_MODEL=llama-3.3-70b-versatile


# ── HTTP API ─────────────────────────────────────────────────────────────────
#
# OSA exposes an HTTP API for webhook delivery and external integrations.
# The default port is 8089. Change it if that port is in use.

OSA_HTTP_PORT=8089

# Authentication
# Set to true in production to require a shared secret on all HTTP requests.
# Generate a strong secret: openssl rand -base64 32
OSA_REQUIRE_AUTH=false
# OSA_SHARED_SECRET=your-secret-here


# ── Telegram ─────────────────────────────────────────────────────────────────
#
# To enable the Telegram channel:
# 1. Create a bot via @BotFather on Telegram
# 2. Copy the bot token here
# 3. Set a webhook secret (any random string)
# 4. Set channels.telegram.enabled = true in config.json
#
# TELEGRAM_BOT_TOKEN=123456789:ABC-DEFghijklmno...
# TELEGRAM_WEBHOOK_SECRET=your-webhook-secret-here


# ── Discord ──────────────────────────────────────────────────────────────────
#
# To enable the Discord channel:
# 1. Create an application at https://discord.com/developers
# 2. Add a bot to the application and copy the token
# 3. Set channels.discord.enabled = true in config.json
#
# DISCORD_BOT_TOKEN=your-discord-bot-token


# ── Slack ────────────────────────────────────────────────────────────────────
#
# To enable the Slack channel:
# 1. Create a Slack app at https://api.slack.com/apps
# 2. Enable Socket Mode and copy the app token (xapp-...)
# 3. Install the app to your workspace and copy the bot token (xoxb-...)
# 4. Set channels.slack.enabled = true in config.json
#
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...


# ── Web Search ───────────────────────────────────────────────────────────────
#
# OSA uses Brave Search for web lookups via the web_search tool.
# Free tier: 2,000 queries/month — sufficient for most personal use.
# Sign up at: https://brave.com/search/api/
#
# BRAVE_API_KEY=BSA...


# ── Agent Tuning ─────────────────────────────────────────────────────────────
#
# These override the defaults in config.json.
# Leave commented out to use config.json values.

# Max tool-calling iterations per message (default: 20)
# Increase for complex multi-step tasks. Decrease to limit runaway loops.
# OSA_MAX_ITERATIONS=20

# LLM temperature (default: 0.7)
# Lower (0.1-0.4) = more deterministic. Higher (0.8-1.0) = more creative.
# OSA_TEMPERATURE=0.7

# Maximum tokens per LLM response (default: 4096)
# OSA_MAX_TOKENS=4096

# Noise filter threshold (default: 0.6)
# Signals below this entropy score are treated as low-priority.
# Lower = more permissive (fewer things filtered). Higher = stricter.
# OSA_NOISE_THRESHOLD=0.6


# ── Paths ────────────────────────────────────────────────────────────────────
#
# Override the default OSA configuration directory.
# Default: ~/.osa
# Useful if running multiple OSA instances or in a non-standard environment.
#
# OSA_CONFIG_DIR=~/.osa
